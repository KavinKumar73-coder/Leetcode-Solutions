# Difficulty : Easy
# Time Complexity : O(n)
# Space Complexty : O(1)

``pyhton 

class Solution(object):
    def finalValueAfterOperations(self, operations):
        x=0
        for i in operations:
            if "+" in i:
                x+=1
            else:
                x-=1
        return x

Input :
operations = ["--X","X++","X++"]
Output :
1
Expected :
1
